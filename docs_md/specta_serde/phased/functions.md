# Functions in specta_serde::phased

```rust
/**
`phased` -- Builds an explicit phased [`DataType`] from precomputed serialize and deserialize shapes.
*/
pub fn phased(serialize: specta::datatype::DataType, deserialize: specta::datatype::DataType) -> specta::datatype::DataType
```

