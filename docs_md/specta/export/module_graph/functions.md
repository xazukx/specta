# Functions in specta::export::module_graph

```rust
/**
`build_module_graph` -- Build a module tree from a flat collection of types and constants.
*/
pub fn build_module_graph<'a>(types: &crate::Types, constants: &crate::Constants) -> Module<''a>
```

