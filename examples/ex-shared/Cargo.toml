[package]
name = "ex-shared"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
specta = { workspace = true, features = ["derive", "collect"] }
specta-macros = { workspace = true, features = ["serde"] }
serde = { workspace = true, features = ["derive"] }
